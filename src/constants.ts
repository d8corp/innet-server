export enum ApiValidationError {
  number = 'number',
  date = 'date',
  integer = 'integer',
  minimum = 'minimum',
  minDate = 'minDate',
  maxDate = 'maxDate',
  maximum = 'maximum',
  values = 'values',
}
